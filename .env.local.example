# .env.local.example — copy to .env.local and fill in real values

# Admin password for /admin page
ADMIN_PASSWORD=your-admin-password-here

# SQLite database (Prisma)
DATABASE_URL="file:./prisma/dev.db"

# --- Subdomain routing (local development) ---
# Modern browsers resolve *.localhost to 127.0.0.1 automatically.
# If not, add these entries to /etc/hosts:
#   127.0.0.1  admin.localhost
#   127.0.0.1  test.localhost
#
# Then access:
#   http://admin.localhost:3000  → admin interface
#   http://test.localhost:3000   → test interface
#
# In production the nginx reverse proxy forwards Host headers to Next.js,
# so no additional env vars are needed for subdomain detection.

# Oculta el botón de Autocompletar al azar en tests. Si es 'true', se muestra.
NEXT_PUBLIC_ENABLE_AUTOFILL=false
